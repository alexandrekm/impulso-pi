{
  "ask": [
    "rm *",
    "sudo *",
    "anyscale * apply*",
    "anyscale * archive*",
    "anyscale * build*",
    "anyscale * create*",
    "anyscale * delete*",
    "anyscale * deploy*",
    "anyscale * push*",
    "anyscale * register*",
    "anyscale * restart*",
    "anyscale * rollback*",
    "anyscale * run_command*",
    "anyscale * set-default*",
    "anyscale * setup*",
    "anyscale * start*",
    "anyscale * submit*",
    "anyscale * tags add*",
    "anyscale * tags remove*",
    "anyscale * terminate*",
    "anyscale * update*",
    "anyscale * add-*",
    "aws s3 cp*",
    "aws s3 mb*",
    "aws s3 mv*",
    "aws s3 rb*",
    "aws s3 rm*",
    "aws s3 sync*",
    "aws s3 website*",
    "aws * allocate-*",
    "aws * apply-*",
    "aws * associate-*",
    "aws * attach-*",
    "aws * authorize-*",
    "aws * cancel-*",
    "aws * copy-*",
    "aws * create-*",
    "aws * delete-*",
    "aws * deploy*",
    "aws * deregister-*",
    "aws * detach-*",
    "aws * disable-*",
    "aws * disassociate-*",
    "aws * enable-*",
    "aws * import-*",
    "aws * modify-*",
    "aws * put-*",
    "aws * reboot-*",
    "aws * register-*",
    "aws * release-*",
    "aws * remove-*",
    "aws * replace-*",
    "aws * restore-*",
    "aws * revoke-*",
    "aws * run-*",
    "aws * schedule-*",
    "aws * start-*",
    "aws * stop-*",
    "aws * tag-*",
    "aws * terminate-*",
    "aws * untag-*",
    "aws * update-*"
  ],
  "deny": []
}
