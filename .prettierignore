node_modules
investigation/
*.md
results.sarif

# Vendored, upstream-managed extensions (Orca/herdr own their formatting;
# re-vendoring should stay a clean diff against upstream).
extensions/orca-integration/
extensions/herdr/
# Vendored fork of @juanbenjumea/pi-dynamic-footer (v0.1.7) + its
# opencode-go-usage dep — keep upstream formatting so re-syncs stay clean;
# theme edits are localized to lib/footer-engine/{segments,layout}.ts.
extensions/pi-dynamic-footer/
